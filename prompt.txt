Jesteś w repo Swift 6 macOS app (Aether IPTV player).

## Stan bieżący
Są uncommitted zmiany w:
- Sources/AetherCore/Design/Colors.swift — dodano #if canImport(SwiftUI) guards
- Sources/AetherCore/Design/Typography.swift — dodano #if canImport(SwiftUI) guards
- Sources/AetherCore/Models/AppearanceMode.swift — przeniesiono colorScheme do #if canImport(SwiftUI)
- Sources/AetherCore/Models/Theme.swift — dodano #if canImport(SwiftUI) guard dla SwiftUI helpers
- Sources/AetherTests/Sprint14Tests.swift — dodano #if canImport(SwiftUI) wokół testThemeAccentColor_notClear

## Cel
Dokończ i upewnij się że wszystko jest idealne:

1. Sprawdź `swift build --target AetherCore --target AetherTests 2>&1` czy kompiluje się bez błędów
2. Jeśli są błędy kompilacji — napraw je
3. Sprawdź czy Sprint14Tests.swift i inne pliki testów są spójne z obecnym API (grep po starych nazwach)
4. Sprawdź czy są inne pliki w AetherCore które importują SwiftUI bez #if canImport(SwiftUI) — jeśli tak, popraw je tak samo
5. Upewnij się że docs/plans/sprint15-vod-series-fix.md jest sensowny (jeśli istnieje — zostaw)
6. Commituj WSZYSTKIE source file changes (nie .build/) z wiadomością: "chore: cross-platform SwiftUI guards + test fixes"
7. Push na GitHub

## Zasady CI
- CI buduje tylko: swift build --target AetherCore --target AetherUI (macOS tylko)  
- Testy: swift test 2>&1 — na Linuxie będą "no such module SwiftUI/AVFoundation" — to norma, ignoruj
- .build/ NIE commituj (jest w .gitignore)

## Ważne
- Nie używaj `swift test` do weryfikacji na tym serwerze (Linux — brak AVFoundation)
- Sprawdź kompilację przez `swift build --target AetherCore 2>&1 | tail -20`
- Po commicie i push — zakończ

Działaj.
